leer(dato);

escribir dato;

si (dato < 0) entonces f := 0 ;
si_no
     i := dato;
     f := 1;
     mientras(i>1) hacer
        f := f * i;
        i := i - 1;
     fin_mientras
fin_si
 
escribir f; 

para i desde 10 hasta -1 paso -2 hacer 
escribir(i);
fin_para